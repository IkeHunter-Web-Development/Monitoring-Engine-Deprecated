# Set URL for your scoped packages, e.g. package with name `@my-org/my-package` will use this URL for download.
"@development-suite:registry"="https://gitlab.com/api/v4/packages/npm/"

# Add the token for the scoped packages URL. This will allow you to download.
"//gitlab.com/api/v4/packages/npm/:_authToken"="${GITLAB_NPM_ACCESS_TOKEN}"
