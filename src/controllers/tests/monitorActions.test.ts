describe('Monitor action controller tests', () => {
  it('should register website response', () => {
    expect(true).toEqual(false)
  })

  it('should register monitor event', () => {
    expect(true).toEqual(false)
  })
})
