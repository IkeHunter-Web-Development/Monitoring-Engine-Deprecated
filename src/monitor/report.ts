import { MonitorType, ReportType } from "./models/types";

export default class ReportManager {
  public static generateReport(monitor: MonitorType, options?: any): ReportType {
    const report: ReportType = {} as ReportType;
    
    return report;
  }
}