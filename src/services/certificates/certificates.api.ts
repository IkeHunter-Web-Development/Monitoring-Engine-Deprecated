/**
 * @fileoverview API connection to the ssl certificate service.
 */
// TODO: Get certificate info

// TODO: if certificate is expired, and previous audit was before expiry, trigger hard refresh

// TODO: if certificate is still expired, trigger alert
